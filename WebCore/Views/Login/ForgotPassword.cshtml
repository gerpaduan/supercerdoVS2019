@model WebCore.Models.PasswordRecoveryRequestVm
@{
    Layout = null;
}
@* Port de Web/Views/Login/ForgotPassword.cshtml, adaptado al diseño propio de WebCore
   (carnisys-login.css, mismo look que Login/Index.cshtml) en vez del sb-admin-2 del clasico --
   2026-09-09, item 1 de los 12 pendientes (Batch E, ver docs/DECISIONS.md). *@
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="theme-color" content="#062d63">
    <title>Recuperar contraseña - CARNISYS</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&amp;display=swap" rel="stylesheet">
    <link href="@Url.Content("~/Content/css/carnisys-login.css")" rel="stylesheet" />
</head>
<body class="carnisys-login">
    <main class="login-page">
        <section class="login-brand-panel" aria-label="Acerca de CARNISYS">
            <a href="@Url.Action("Index", "Landing")" class="login-brand" aria-label="Volver al inicio de CARNISYS">
                <img src="@Url.Content("~/Content/img/CarniSys_Login.png")" alt="CARNISYS" />
            </a>

            <div class="brand-content">
                <span class="brand-eyebrow"><i class="fas fa-balance-scale" aria-hidden="true"></i> Gestión pensada para comercios</span>
                <h1>Recuperá el<br /><strong>acceso</strong> a tu cuenta.</h1>
                <p>Te mandamos un enlace por email para que puedas crear una contraseña nueva.</p>
            </div>

            <div class="brand-footer">
                <i class="fas fa-store" aria-hidden="true"></i>
                <span>Creado desde la experiencia real de comercio.</span>
            </div>
        </section>

        <section class="login-form-panel" aria-labelledby="forgot-title">
            <div class="login-form-wrap">
                <a href="@Url.Action("Index", "Login")" class="back-home"><i class="fas fa-arrow-left" aria-hidden="true"></i> Volver al login</a>

                <div class="login-heading">
                    <span class="login-kicker">Acceso al sistema</span>
                    <h2 id="forgot-title">Recuperar contraseña</h2>
                    <p>Ingresá tu usuario o email. Si corresponde a una cuenta registrada, te enviaremos instrucciones para restablecer tu contraseña.</p>
                </div>

                @if (!string.IsNullOrWhiteSpace(Model.Mensaje))
                {
                    <div class="login-alert login-alert-error" role="alert"><i class="fas fa-exclamation-circle" aria-hidden="true"></i> @Model.Mensaje</div>
                }

                @if (!ViewData.ModelState.IsValid)
                {
                    <div class="login-alert login-alert-error" role="alert">
                        <i class="fas fa-exclamation-circle" aria-hidden="true"></i>
                        @Html.ValidationSummary(false)
                    </div>
                }

                <form asp-controller="Login" asp-action="ForgotPassword" method="post" class="carnisys-login-form" novalidate="novalidate">
                    @Html.AntiForgeryToken()

                    <div class="login-field">
                        @Html.LabelFor(m => m.UsuarioOEmail)
                        <div class="input-shell">
                            <i class="far fa-user" aria-hidden="true"></i>
                            @Html.TextBoxFor(m => m.UsuarioOEmail, new { placeholder = "Usuario o email", autocomplete = "username", autofocus = "autofocus" })
                        </div>
                        @Html.ValidationMessageFor(m => m.UsuarioOEmail, "", new { @class = "field-error" })
                    </div>

                    <button type="submit" class="login-submit">Enviar instrucciones <i class="fas fa-arrow-right" aria-hidden="true"></i></button>
                </form>
            </div>
        </section>
    </main>

    <script src="@Url.Content("~/lib/jquery/dist/jquery.min.js")"></script>
    <script src="@Url.Content("~/lib/jquery-validation/dist/jquery.validate.min.js")"></script>
    <script src="@Url.Content("~/lib/jquery-validation-unobtrusive/dist/jquery.validate.unobtrusive.min.js")"></script>
</body>
</html>
