@model Web.Models.MovimientoEditVm
@using System.Collections.Generic
@using System.Web
@using Entidades

@{
    Layout = "~/Views/Shared/_LayoutBase.cshtml";
    var sucursales = ViewBag.Sucursales as List<Sucursal> ?? new List<Sucursal>();
    var modalProductoData = new ViewDataDictionary
    {
        { "ModalId", "modalBuscarProductoMovimiento" },
        { "ApiUrl", Url.Action("BuscarProducto", "Movimientos") },
        { "Titulo", "Buscar producto para Movimiento" },
        { "MostrarPrecio", "false" }
    };
}

<div class="container-fluid px-2 px-md-3 movimiento-page movimiento-form-compact" data-movimiento-page="1">
    <div id="movimientoDraftBanner" class="alert alert-warning d-none" role="alert">
        Se encontró un borrador local de este movimiento.
        <button type="button" class="btn btn-sm btn-warning ml-2" data-action="restore-draft">Recuperar</button>
        <button type="button" class="btn btn-sm btn-outline-dark ml-2" data-action="clear-draft">Limpiar borrador</button>
    </div>

    <div class="d-flex flex-wrap align-items-center justify-content-between mb-3 gap-2">
        <a class="btn btn-outline-secondary" href="@Url.Action("Index", "Movimientos")">
            <i class="fas fa-arrow-left mr-1"></i> Volver
        </a>
        @if (Model.EsEdicion)
        {
            <button type="button" class="btn btn-outline-primary" id="btnImprimirMovimiento">
                <i class="fas fa-print mr-1"></i> Imprimir
            </button>
        }
    </div>

    <div class="card shadow-sm movimiento-panel-card">
        <div class="card-body">
            <form id="formMovimiento" method="post" action="@Url.Action("Guardar", "Movimientos")" novalidate>
                @Html.AntiForgeryToken()
                @Html.HiddenFor(m => m.IdMovimiento)
                @Html.HiddenFor(m => m.EmpresaCuit)
                <div id="lineasHiddenContainer"></div>

                <div class="row">
                    <div class="col-12 col-lg-3">
                        <div class="form-group">
                            <label for="IdSucursalOrigen">Sucursal origen</label>
                            <select id="IdSucursalOrigen" name="IdSucursalOrigen" class="form-control">
                                <option value="0">Seleccione...</option>
                                @foreach (var sucursal in sucursales)
                                {
                                    <option value="@sucursal.IdSucursal" @(sucursal.IdSucursal == Model.IdSucursalOrigen ? "selected" : "")>@sucursal.SucursalNombre</option>
                                }
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-lg-3">
                        <div class="form-group">
                            <label for="IdSucursalDestino">Sucursal destino</label>
                            <select id="IdSucursalDestino" name="IdSucursalDestino" class="form-control">
                                <option value="0">Seleccione...</option>
                                @foreach (var sucursal in sucursales)
                                {
                                    <option value="@sucursal.IdSucursal" @(sucursal.IdSucursal == Model.IdSucursalDestino ? "selected" : "")>@sucursal.SucursalNombre</option>
                                }
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-lg-3">
                        <div class="form-group">
                            <label for="FechaMovimiento">Fecha</label>
                            <input type="datetime-local"
                                   id="FechaMovimiento"
                                   name="FechaMovimiento"
                                   class="form-control"
                                   value="@Model.FechaMovimiento.ToString("yyyy-MM-ddTHH:mm")" />
                            @Html.Partial("_AdvertenciaPermisoFecha")
                        </div>
                    </div>
                </div>

                <hr />

                <div class="row">
                    <div class="col-12 col-xl-8">
                        <div class="border rounded p-3 mb-3 movimiento-section-box">
                            <div class="d-flex flex-wrap justify-content-between align-items-center mb-3">
                                <h6 class="font-weight-bold text-uppercase text-muted mb-0 movimiento-section-title">Agregar producto</h6>
                                <div class="d-flex flex-wrap align-items-center gap-3">
                                    <div class="custom-control custom-switch mr-2">
                                        <input type="checkbox" class="custom-control-input" id="chkBalanzaLinea" checked />
                                        <label class="custom-control-label" for="chkBalanzaLinea">Balanza</label>
                                    </div>
                                    <button type="button" class="btn btn-sm btn-outline-secondary" id="btnVerAcumulados">
                                        <i class="fas fa-layer-group mr-1"></i> Ver acumulados
                                    </button>
                                </div>
                            </div>

                            <div id="balanzaStatusWrapMovimiento">
                                <div class="small text-muted mb-2">
                                    Conexión balanza: <span id="estadoBalanzaMovimiento">Desconocida</span>
                                </div>
                                <div class="progress mb-3" style="height:4px;">
                                    <div id="barraBalanzaMovimiento" class="progress-bar" role="progressbar" style="width:0%"></div>
                                </div>
                            </div>

                            <div id="movimientoFeedback" class="alert alert-success py-2 px-3 mb-3 d-none"></div>
                            <div id="movimientoWarning" class="alert alert-warning py-2 px-3 mb-3 d-none"></div>

                            <div class="row align-items-end movimiento-producto-row">
                                <div class="col-12 col-md-4 col-lg-3 movimiento-col-codigo">
                                    <div class="form-group">
                                        <label for="txtCodigoProducto">Código</label>
                                        <input type="number" id="txtCodigoProducto" class="form-control movimiento-input-codigo" min="0" step="1" autocomplete="off" />
                                    </div>
                                </div>
                                <div class="col-12 col-md-8 col-lg movimiento-col-producto">
                                    <div class="form-group">
                                        <label for="txtProductoNombre">Producto</label>
                                        <div class="input-group">
                                            <input type="hidden" id="txtProductoId" />
                                            <input type="hidden" id="txtProductoPesable" />
                                            <input type="hidden" id="txtProductoTipo" />
                                            <input type="hidden" id="txtProductoPromedio" />
                                            <input type="text" id="txtProductoNombre" class="form-control" readonly />
                                            <div class="input-group-append">
                                                <button type="button" class="btn btn-outline-primary" id="btnBuscarProducto" title="Buscar producto (F10)">
                                                    <i class="fas fa-search"></i>
                                                    <span class="d-none d-lg-inline ml-1">F10</span>
                                                </button>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-6 col-md-3 col-lg-2 movimiento-col-cantidad">
                                    <div class="form-group">
                                        <label for="txtCantUnidad">Cant.Un.</label>
                                        <input type="number" id="txtCantUnidad" class="form-control movimiento-input-cantidad" min="0" step="1" />
                                    </div>
                                </div>
                                <div class="col-6 col-md-3 col-lg-2 movimiento-col-cantidad">
                                    <div class="form-group">
                                        <label for="txtCantKgs">Kgs.</label>
                                        <input type="text" id="txtCantKgs" class="form-control solo-decimal movimiento-input-cantidad" inputmode="decimal" autocomplete="off" />
                                    </div>
                                </div>
                                <div class="col-12 col-md-6 col-lg-auto movimiento-col-accion">
                                    <div class="form-group">
                                        <label class="d-block invisible mb-0">&nbsp;</label>
                                        <button type="button" class="btn btn-primary movimiento-btn-add" id="btnAgregarProducto" title="Agregar producto" aria-label="Agregar producto">
                                            <i class="fas fa-plus"></i>
                                        </button>
                                    </div>
                                </div>
                            </div>

                            <div class="custom-control custom-checkbox mb-3 d-none" id="wrapPermitirIngreso">
                                <input type="checkbox" class="custom-control-input" id="chkPermitirIngreso" />
                                <label class="custom-control-label" for="chkPermitirIngreso">Permitir ingreso</label>
                                <small class="form-text text-muted">Presione <strong>+</strong> para registrar.</small>
                            </div>

                        </div>

                        <div class="table-responsive">
                            <table class="table table-bordered table-sm movimiento-table-compact" id="tablaLineasMovimiento">
                                <thead class="thead-light">
                                    <tr>
                                        <th style="width:110px;">Código</th>
                                        <th>Producto</th>
                                        <th class="text-right" style="width:8ch;">Cant.Un.</th>
                                        <th class="text-right" style="width:8ch;">Kilos</th>
                                        <th class="text-center" style="width:8ch;">Balanza</th>
                                        <th class="text-center" style="width:8ch;">Permitir ingreso</th>
                                        <th style="width:70px;">Acción</th>
                                    </tr>
                                </thead>
                                <tbody></tbody>
                            </table>
                        </div>
                    </div>

                    <div class="col-12 col-xl-4">
                        <div class="card bg-light movimiento-panel-card movimiento-summary-card">
                            <div class="card-body">
                                <h6 class="font-weight-bold text-uppercase text-muted mb-2 movimiento-section-title">Resumen</h6>
                                <div class="d-flex justify-content-between">
                                    <span>Total items</span>
                                    <strong id="lblTotalItems">0</strong>
                                </div>
                                <div class="d-flex justify-content-between">
                                    <span>Total unidades</span>
                                    <strong id="lblTotalUnidades">0</strong>
                                </div>
                                <div class="d-flex justify-content-between">
                                    <span>Total kilos</span>
                                    <strong id="lblTotalKilos">0.000</strong>
                                </div>
                                <hr />
                                <div class="small text-muted">
                                    Usuario: <strong>@Model.UsuarioNombre</strong>
                                </div>
                            </div>
                        </div>
                        <div class="card movimiento-panel-card mt-3">
                            <div class="card-body">
                                <div class="form-group mb-0">
                                    <label for="Observaciones">Observaciones</label>
                                    <textarea id="Observaciones"
                                              name="Observaciones"
                                              class="form-control js-auto-resize"
                                              rows="2"
                                              style="min-height: calc(1.5em * 2 + 1rem); overflow:hidden; resize:none;"
                                              maxlength="250">@Model.Observaciones</textarea>
                                </div>
                            </div>
                        </div>
                        @if (Model.EsEdicion)
                        {
                            <div class="card movimiento-panel-card mt-3 movimiento-meta-card">
                                <div class="card-body">
                                    <label class="d-block mb-2">Datos del movimiento</label>
                                    <div class="movimiento-meta-grid">
                                        <div class="movimiento-meta-item">
                                            <div class="small text-muted movimiento-meta-label">Movimiento origen</div>
                                            <div class="font-weight-bold">@Model.IdMovimientoOrigen</div>
                                        </div>
                                        <div class="movimiento-meta-item">
                                            <div class="small text-muted movimiento-meta-label">Movimiento destino</div>
                                            <div class="font-weight-bold">@Model.IdMovimientoDestino</div>
                                        </div>
                                        <div class="movimiento-meta-item">
                                            <div class="small text-muted movimiento-meta-label">Creado</div>
                                            <div class="font-weight-bold">@Model.Creado</div>
                                            <div class="small text-muted">@Model.CreadoPor</div>
                                        </div>
                                        <div class="movimiento-meta-item">
                                            <div class="small text-muted movimiento-meta-label">Actualizado</div>
                                            <div class="font-weight-bold">@Model.Actualizado</div>
                                            <div class="small text-muted">@Model.ActualizadoPor</div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                    </div>
                </div>

                <div class="mt-3 d-flex flex-wrap align-items-center">
                    @if (Model.EsEdicion)
                    {
                        <button type="button"
                                class="btn btn-primary mr-2 mb-2"
                                id="btnHabilitarEdicionMovimiento"
                                data-edit-readonly-ignore="true">
                            <i class="fas fa-edit mr-1"></i> Modificar
                        </button>
                    }
                    <button type="submit" class="btn btn-success mr-2 mb-2 @(Model.EsEdicion ? "d-none" : "")" id="btnGuardarMovimiento">
                        <i class="fas fa-save mr-1"></i> Guardar movimiento
                    </button>
                    <a href="@Url.Action("Index", "Movimientos")" class="btn btn-outline-secondary mb-2">
                        Cancelar
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>

<div class="modal fade" id="modalAcumuladosMovimiento" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-scrollable" role="document">
        <div class="modal-content">
            <div class="modal-header bg-secondary text-white">
                <h5 class="modal-title">Acumulados del movimiento</h5>
                <button type="button" class="close text-white" data-dismiss="modal" aria-label="Cerrar">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div class="table-responsive">
                    <table class="table table-sm table-bordered mb-0 movimiento-table-compact">
                        <thead class="thead-light">
                            <tr>
                                <th style="width:110px;">Código</th>
                                <th>Producto</th>
                                <th class="text-right" style="width:110px;">Cant.Un.</th>
                                <th class="text-right" style="width:110px;">Kilos</th>
                            </tr>
                        </thead>
                        <tbody id="tbodyAcumuladosMovimiento"></tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

@Html.Partial("~/Views/Productos/_BuscarProductoModal.cshtml", modalProductoData)
@Html.Partial("~/Views/Movimientos/_ModalPostMovimiento.cshtml")

<style>
    .movimiento-page .movimiento-panel-card {
        border: 1px solid #e9ecef;
        border-radius: .75rem;
    }

    .movimiento-page .movimiento-panel-card > .card-body,
    .movimiento-page .movimiento-panel-card .card-body {
        padding: .82rem .96rem;
    }

    .movimiento-page .movimiento-section-box {
        border: 1px solid #e9ecef !important;
        border-radius: .7rem !important;
        padding: .8rem .92rem !important;
        margin-bottom: .85rem !important;
    }

    .movimiento-page .movimiento-form-compact label,
    .movimiento-page .movimiento-section-box label,
    .movimiento-page .movimiento-panel-card label {
        font-size: .76rem;
        font-weight: 600;
        color: #495057;
        margin-bottom: .18rem;
    }

    .movimiento-page .movimiento-form-compact .form-control,
    .movimiento-page .movimiento-form-compact .input-group-text,
    .movimiento-page .movimiento-form-compact .btn:not(.btn-sm),
    .movimiento-page .movimiento-section-box .form-control,
    .movimiento-page .movimiento-section-box .input-group-text,
    .movimiento-page .movimiento-section-box .btn:not(.btn-sm),
    .movimiento-page .movimiento-panel-card .form-control,
    .movimiento-page .movimiento-panel-card .input-group-text,
    .movimiento-page .movimiento-panel-card .btn:not(.btn-sm) {
        min-height: 2.15rem;
        font-size: .84rem;
        padding: .28rem .56rem;
        border-radius: .52rem;
    }

    .movimiento-page .movimiento-section-title {
        font-size: .72rem;
        letter-spacing: .04em;
        line-height: 1.1;
        margin-bottom: .55rem !important;
    }

    .movimiento-page .movimiento-form-compact .custom-control-label,
    .movimiento-page .movimiento-section-box .custom-control-label,
    .movimiento-page .movimiento-panel-card .custom-control-label,
    .movimiento-page .movimiento-form-compact .small,
    .movimiento-page .movimiento-form-compact .text-muted,
    .movimiento-page .movimiento-section-box .small,
    .movimiento-page .movimiento-section-box .text-muted,
    .movimiento-page .movimiento-panel-card .small,
    .movimiento-page .movimiento-panel-card .text-muted {
        font-size: .76rem !important;
    }

    .movimiento-page .movimiento-section-box h6.text-uppercase,
    .movimiento-page .movimiento-panel-card h6.text-uppercase {
        font-size: .72rem;
        letter-spacing: .04em;
        line-height: 1.1;
    }

    .movimiento-page .movimiento-meta-label {
        font-size: .68rem !important;
        line-height: 1.05;
        margin-bottom: .14rem !important;
        text-transform: uppercase;
        letter-spacing: .03em;
    }

    .movimiento-page .movimiento-summary-card .card-body {
        padding: .62rem .78rem;
    }

    .movimiento-page .movimiento-meta-card .card-body {
        min-height: calc(1.5em * 2 + 3.2rem);
    }

    .movimiento-page .movimiento-meta-grid {
        display: grid;
        grid-template-columns: 1fr;
        gap: .65rem;
    }

    .movimiento-page .movimiento-meta-item {
        min-width: 0;
    }

    .movimiento-page .movimiento-meta-card .font-weight-bold {
        font-size: .82rem;
        line-height: 1.15;
    }

    .movimiento-page .movimiento-meta-card .small.text-muted:not(.movimiento-meta-label) {
        font-size: .7rem !important;
        line-height: 1.1;
    }

    .movimiento-page .movimiento-producto-row {
        margin-left: -.3rem;
        margin-right: -.3rem;
    }

    .movimiento-page .movimiento-producto-row > div {
        padding-left: .3rem;
        padding-right: .3rem;
    }

    .movimiento-page .movimiento-input-codigo {
        width: 100%;
        min-width: 100%;
    }

    .movimiento-page .movimiento-input-cantidad {
        width: 100%;
        min-width: 100%;
    }

    .movimiento-page .movimiento-btn-add {
        width: 100%;
    }

    .movimiento-page .movimiento-table-compact {
        font-size: .82rem;
    }

    .movimiento-page .movimiento-table-compact th,
    .movimiento-page .movimiento-table-compact td {
        padding: .36rem .48rem;
        vertical-align: middle;
        line-height: 1.12;
    }

    .movimiento-page .movimiento-table-compact thead th,
    .movimiento-page .movimiento-table-compact > thead > tr > th {
        font-size: .66rem !important;
        line-height: 1.02;
        padding-top: .32rem;
        padding-bottom: .32rem;
        text-transform: uppercase;
        letter-spacing: .02em;
    }

    .movimiento-page #tablaLineasMovimiento thead th[data-sort-key] {
        user-select: none;
        transition: background-color .15s ease, color .15s ease, box-shadow .15s ease;
    }

    .movimiento-page #tablaLineasMovimiento thead th[data-sort-key]:hover {
        background: rgba(0, 0, 0, .045);
    }

    .movimiento-page #tablaLineasMovimiento thead th.movimiento-sort-active {
        background: rgba(13, 110, 253, .12);
        color: #0b57c0;
        box-shadow: inset 0 -2px 0 rgba(13, 110, 253, .45);
    }

    .dark-mode .movimiento-page #tablaLineasMovimiento thead th[data-sort-key]:hover,
    body.dark-mode .movimiento-page #tablaLineasMovimiento thead th[data-sort-key]:hover {
        background: rgba(255, 255, 255, .08);
    }

    .dark-mode .movimiento-page #tablaLineasMovimiento thead th.movimiento-sort-active,
    body.dark-mode .movimiento-page #tablaLineasMovimiento thead th.movimiento-sort-active {
        background: rgba(84, 160, 255, .18);
        color: #d9e9ff;
        box-shadow: inset 0 -2px 0 rgba(149, 197, 255, .55);
    }

    .movimiento-page .movimiento-table-compact .btn-sm {
        padding: .18rem .42rem;
        line-height: 1.12;
        font-size: .76rem;
    }

    @@media (min-width: 1200px) {
        .movimiento-page .movimiento-col-codigo {
            -ms-flex: 0 0 25%;
            flex: 0 0 25%;
            max-width: 25%;
        }

        .movimiento-page .movimiento-col-producto {
            -ms-flex: 1 1 0%;
            flex: 1 1 0%;
            max-width: none;
        }

        .movimiento-page .movimiento-col-cantidad {
            -ms-flex: 0 0 16.666667%;
            flex: 0 0 16.666667%;
            max-width: 16.666667%;
        }

        .movimiento-page .movimiento-col-accion {
            -ms-flex: 0 0 auto;
            flex: 0 0 auto;
            width: auto;
            max-width: none;
        }

        .movimiento-page .movimiento-input-codigo {
            width: 100%;
            min-width: 100%;
            max-width: none;
        }

        .movimiento-page .movimiento-input-cantidad {
            width: 100%;
            min-width: 100%;
            max-width: none;
        }

        .movimiento-page .movimiento-btn-add {
            width: 2.15rem;
            min-width: 2.15rem;
            padding-left: 0;
            padding-right: 0;
        }

        .movimiento-page .mb-3 {
            margin-bottom: .85rem !important;
        }

        .movimiento-page .movimiento-panel-card > .card-body,
        .movimiento-page .movimiento-panel-card .card-body {
            padding: .74rem .86rem;
        }

        .movimiento-page .movimiento-meta-card .card-body {
            min-height: calc(1.5em * 2 + 3rem);
        }

        .movimiento-page .movimiento-meta-card .font-weight-bold {
            font-size: .79rem;
        }

        .movimiento-page .movimiento-section-box {
            padding: .72rem .84rem !important;
        }

        .movimiento-page .movimiento-form-compact .form-control,
        .movimiento-page .movimiento-form-compact .input-group-text,
        .movimiento-page .movimiento-form-compact .btn:not(.btn-sm),
        .movimiento-page .movimiento-section-box .form-control,
        .movimiento-page .movimiento-section-box .input-group-text,
        .movimiento-page .movimiento-section-box .btn:not(.btn-sm),
        .movimiento-page .movimiento-panel-card .form-control,
        .movimiento-page .movimiento-panel-card .input-group-text,
        .movimiento-page .movimiento-panel-card .btn:not(.btn-sm) {
            min-height: 2.02rem;
            font-size: .81rem;
            padding: .24rem .5rem;
        }

        .movimiento-page .movimiento-table-compact {
            font-size: .79rem;
        }

        .movimiento-page .movimiento-table-compact thead th,
        .movimiento-page .movimiento-table-compact > thead > tr > th {
            font-size: .63rem !important;
        }
    }

    @@media (min-width: 768px) and (max-width: 1199.98px) {
        .movimiento-page .movimiento-meta-grid {
            grid-template-columns: repeat(2, minmax(0, 1fr));
        }
    }
</style>

@section Scripts {
    <script>
        window.movimientosConfig = {
            draftKey: 'movimiento_draft_@(Model.EsEdicion ? Model.IdMovimiento.ToString() : "new")',
            urlBuscarProductoPorCodigo: '@Url.Action("BuscarProductoPorCodigo", "Movimientos")',
            redirectUrl: '@Url.Action("Index", "Movimientos")',
            imprimirUrl: '@(Model.EsEdicion ? Url.Action("ImprimirTicket", "Movimientos", new { id = Model.IdMovimiento }) : "")',
            imprimirPayloadUrl: '@(Model.EsEdicion ? Url.Action("ImprimirTicketPayload", "Movimientos", new { id = Model.IdMovimiento }) : "")',
            pdfUrl: '@(Model.EsEdicion ? Url.Action("ImprimirPdf", "Movimientos", new { id = Model.IdMovimiento }) : "")',
            whatsappTexto: '@(Model.EsEdicion ? HttpUtility.JavaScriptStringEncode("Movimiento " + Model.IdMovimiento + " - " + Url.Action("ImprimirPdf", "Movimientos", new { id = Model.IdMovimiento }, Request.Url.Scheme)) : "")',
            balanzaUrl: 'http://localhost:5100/peso',
            soloLecturaInicial: @(Model.EsEdicion ? "true" : "false")
        };
        window.movimientoLineasIniciales = @Html.Raw(Json.Encode(Model.Lineas ?? new List<Web.Models.MovimientoLineaVm>()));
    </script>
    <script src="@Url.Content("~/Scripts/carnisys.balanza.js?v=6")"></script>
    <script src="@Url.Content("~/Scripts/app/print-agent.js?v=3")"></script>
    <script src="@Url.Content("~/Scripts/app/modal-postmovimiento.js?v=7")"></script>
    <script src="@Url.Content("~/Scripts/app/movimientos.js?v=20")"></script>
    <script src="@Url.Content("~/Scripts/app/edit-readonly.js?v=1")"></script>
    <script>
        window.EditReadOnly && window.EditReadOnly.init({
            formSelector: "#formMovimiento",
            soloLecturaInicial: @(Model.EsEdicion ? "true" : "false"),
            modifyButtonSelector: "#btnHabilitarEdicionMovimiento",
            saveButtonSelector: "#btnGuardarMovimiento",
            onStateChanged: function (readOnly) {
                if (window.MovimientosEdit && typeof window.MovimientosEdit.setReadOnly === "function") {
                    window.MovimientosEdit.setReadOnly(readOnly);
                }
            }
        });
    </script>
}
