@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <title>CarniSysWeb - Login</title>

    <!-- Fuentes y estilos SB Admin 2 -->
    <link href="~/Content/vendor/fontawesome-free/css/all.min.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css?family=Nunito:200,300,400,600,700,800,900"
          rel="stylesheet">

    <!-- Estilos principales -->
    <link href="~/Content/css/sb-admin-2.min.css" rel="stylesheet" />

    <!-- Imagen de fondo izquierda (opcional) -->
    <style>
        @*.bg-login-image {
            background: url('@Url.Content("~/Content/img/CarniSys_Logo_sinSlogan.png")');
            background-position: center;
            background-size: cover;
            background-repeat: no-repeat;
        }*@
        .login-logo {
            width: 100%; /* MISMO ancho que los inputs */
            max-width: 100%; /* Evita que se achique más */
            display: block;
            margin-bottom: 20px;
        }

    </style>
</head>
<body class="bg-gradient-primary">

    <div class="container">

        <!-- ROW CENTRAL -->
        <div class="row justify-content-center">

            <div class="col-xl-6 col-lg-7 col-md-9">

                <div class="card o-hidden border-0 shadow-lg my-5">
                    <div class="card-body p-5">

                        <!-- CONTENIDO IZQUIERDA -->

                        @*<img src="@Url.Content("~/Content/img/CarniSys_Login.png")"
                             alt="Logo"
                             style="max-width: 250px; display:block; margin-bottom: 20px;" />*@
                        <img src="@Url.Content("~/Content/img/CarniSys_Login.png")"
                             alt="Logo"
                             class="login-logo" />
                        <h1 class="h4 text-gray-900 mb-4">Iniciar sesión</h1>

                        @* Mostrar error *@
                        @if (ViewBag.Error != null)
                        {
                            <div class="alert alert-danger">
                                @ViewBag.Error
                            </div>
                        }

                        @using (Html.BeginForm("Index", "Login", FormMethod.Post, new { @class = "user" }))
                        {
                            <div class="form-group">
                                <input type="text" name="usuario"
                                       class="form-control form-control-user"
                                       placeholder="Usuario o Email" required />
                            </div>

                            <div class="form-group">
                                <input type="password" name="clave"
                                       class="form-control form-control-user"
                                       placeholder="Contraseña" required />
                            </div>

                            <button type="submit" class="btn btn-primary btn-user btn-block">
                                Ingresar
                            </button>
                        }

                    </div>
                </div>

            </div>

        </div>

    </div>

</body>


</html>

@*<!DOCTYPE html>
    <html lang="es">
    <head>
        <meta charset="utf-8" />
        <title>Login - CarniSys</title>
        <link href="~/Content/css/sb-admin-2.min.css" rel="stylesheet" />
    </head>
    <body class="bg-gradient-primary">

        <div class="container">
            <div class="row justify-content-center">
                <div class="col-xl-5 col-lg-6 col-md-8">
                    <div class="card o-hidden border-0 shadow-lg my-5">
                        <div class="card-body p-5">
                            <div class="text-center mb-4">
                                <h1 class="h4 text-gray-900 mb-2">Iniciar sesión</h1>
                            </div>

                            @if (ViewBag.Error != null)
                            {
                                <div class="alert alert-danger">@ViewBag.Error</div>
                            }

                            @using (Html.BeginForm("Index", "Login", FormMethod.Post))
                            {
                                <div class="form-group">
                                    <input type="text" name="usuario" class="form-control form-control-user" placeholder="Usuario" required />
                                </div>
                                <div class="form-group">
                                    <input type="password" name="clave" class="form-control form-control-user" placeholder="Contraseña" required />
                                </div>
                                <button type="submit" class="btn btn-primary btn-user btn-block">
                                    Ingresar
                                </button>
                            }

                        </div>
                    </div>
                </div>
            </div>
        </div>

        <script src="~/Scripts/jquery.min.js"></script>
        <script src="~/Scripts/bootstrap.bundle.min.js"></script>
        <script src="~/Scripts/sb-admin-2.min.js"></script>
    </body>
    </html>*@

