<div class="modal fade" id="modalBuscarPersona" tabindex="-1" role="dialog">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">

            <!-- HEADER -->
            <div class="modal-header bg-primary text-white">
                <h5 class="modal-title">
                    <i class="fas fa-user-search"></i> Buscar Persona
                </h5>
                <button type="button" class="close text-white" data-dismiss="modal">
                    <span>&times;</span>
                </button>
            </div>

            <!-- BODY -->
            <div class="modal-body">

                <!-- Filtro único -->
                <div class="form-group mb-3">
                    <input type="text"
                           id="filtroPersona"
                           class="form-control"
                           placeholder="Buscar por razón social, CUIT o identificación">
                </div>

                <!-- Tabla personas -->
                <div class="table-responsive">
                    <table class="table table-hover table-sm">
                        <thead class="thead-light">
                            <tr>
                                <th>Razón Social</th>
                                <th>CUIT</th>
                                <th>Identificación</th>
                                <th class="text-right">Seleccionar</th>
                            </tr>
                        </thead>
                        <tbody id="tablaPersonas"></tbody>
                    </table>
                </div>

            </div>

        </div>
    </div>
</div>

<script>
    const personaUrls = {
        listar: '@Url.Action("Listar", "Personas")'
    };
</script>
